-- BITWISE HUB UNIVERSAL LOADER
-- By IngVzzz

-- 1. NUNGGU SAMPAI GAME SELESAI LOAD
if game:GetService('ContentProvider').RequestQueueSize > 10 then           
    repeat task.wait()
    until game:GetService('ContentProvider').RequestQueueSize <= 10
end

-- 2. ANTI AFK
game:GetService("Players").LocalPlayer.Idled:connect(function()
    game:GetService("VirtualUser"):ClickButton2(Vector2.new())
end)

-- 3. SETUP FOLDER UNTUK NYIMPAN DATA
local shared = {
    version = "V3.0.0";
    folders = {
        main = 'BitwiseHub',
        games = 'BitwiseHub/Games',
    };
}

-- 4. BIKIN FOLDER KALO BELUM ADA
for _, folder in pairs(shared.folders) do
    if not isfolder(folder) then
        makefolder(folder)
    end
end

-- 5. DATABASE GAME (Place ID → URL Script)
local games = {
    [{93978595733734}] = "https://raw.githubusercontent.com/IngVzzz/VIOLENCE-DISTRICT/refs/heads/main/violence%20district%20fav",
}

-- 6. CEK PLACE ID & LOAD SCRIPT YANG SESUAI
for ids, url in next, games do
    if table.find(ids, game.PlaceId) then
        task.wait()
        loadstring(game:HttpGet(url))()
    end
end
